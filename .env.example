OPENAI_API_KEY="<your_openai_api_key>"
# provide aws creds or provide them in your environment another way
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_DEFAULT_REGION=""

# Optional Bedrock bearer token for Mantle (otherwise uses AWS creds via short-term token)
# AWS_BEARER_TOKEN_BEDROCK=""

# If set, you can use Grok
XAI_API_KEY=""
DD_API_KEY="optional datadog api key for traces"

GOOGLE_API_KEY=""

# Optional Ollama overrides (defaults: http://localhost:11434/v1 and apiKey "ollama")
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_API_KEY=ollama

# Port for the example server and e2e tests (default: 3456)
E2E_PORT=""
